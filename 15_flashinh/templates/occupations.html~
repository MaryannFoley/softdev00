<!DOCTYPE html>
<html>
  <title> {{Title}} </title>
  <h2>
    {{Heading}}
  </h2>
  <body>
    <table>
      <tr>
	<th> {{ col1 }} </th>
	<th> {{ col2 }} </th>
      </tr>
      {% for x in y %}
      <tr>
        <td> {{ x }} </td>
      </tr>
      {% endfor %}
    </table>
  </body>
</html>
